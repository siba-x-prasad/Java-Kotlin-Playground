package com.swasi.play.coroutines.context


/**
 * Coroutine Context
 *
 * From the above, we know that job and dispatcher are both Coroutine Context.
 * Other than job and dispatcher, there are other Coroutine Context,
 * such as CoroutineName, ContinuationInterceptor, and CoroutineExceptionHandler.

    Coroutine context is just a set of elements (which are also Contexts) that provide the context for the coroutine.

 Job, Dispatcher, Name, Contineous Intercepter,  Exception Handler comes under Coroutine Context

 *
 */
fun main() {

}